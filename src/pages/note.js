import React from 'react';

import Layout from '../components/layout';

const Note = ({location}) => {
  return (
    <Layout title='读书笔记JS上' location={location}>
      <div> 读书笔记-JS上 </div>
    </Layout>
  )
};

export default Note;
